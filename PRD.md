# 课堂 AI 图片生成互动工具 PRD

## 1. 产品目标
为小学课堂设计一个 AI 图片生成互动工具，让全班学生分组使用平板，通过语音输入提示词生成 AI 图片，老师可通过管理端查看所有学生的作品。

## 2. 用户画像
| 角色 | 描述 | 使用场景 |
|------|------|---------|
| 老师 | 课堂组织者，导入学生名单、分组、查看作品 | 课前准备 + 课中监控 |
| 学生 | 使用平板操作，选择自己姓名 → 语音输入 → 生成图片 | 课堂活动中 |

## 3. 需求池

### P0（必须）
- [ ] Excel 上传导入学生名单，老师可在导入时指定每组包含哪些学生
- [ ] 学生端 `/sk`：显示全部 8 组的学生姓名，学生点击自己姓名
- [ ] 学生端语音输入：网页语音识别（Web Speech API），转成中文文本
- [ ] 学生端图片生成：语音文本 → 调即梦 API → 展示生成的图片
- [ ] 老师端 `/sk/ls`：以图片墙形式展示全班所有学生生成的图片
- [ ] 数据存储：学生信息、分组、生成记录持久化存储

### P1（重要）
- [ ] 每组独立平板（互不干扰，组与组之间数据隔离？实际上在同页面不同区域展示）
- [ ] 学生可多次生成，历史记录保存在画廊中
- [ ] 老师端可刷新查看最新生成图片

### P2（可以有）
- [ ] 图片放大查看
- [ ] 删除不合适的图片

## 4. 页面设计

### 4.1 学生端 `/sk`
```
┌─────────────────────────────────────────────┐
│  [Logo] 欢迎使用 AI 绘画小课堂              │
├─────────────────────────────────────────────┤
│  请选择你的姓名：                            │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐      │
│  │ 张三  │ │ 李四  │ │ 王五  │ │ 赵六  │      │
│  └──────┘ └──────┘ └──────┘ └──────┘      │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐      │
│  │ 孙七  │ │ 周八  │ │ 吴九  │ │ 郑十  │      │
│  └──────┘ └──────┘ └──────┘ └──────┘      │
│     （按组排列，每组学生在一起）              │
├─────────────────────────────────────────────┤
│  已选择：张三                                │
│  [🎤 按住说话] 或  [⌨️ 打字输入]           │
│  ┌─────────────────────────────────┐        │
│  │ 提示词：一只会飞的小猫...       │        │
│  └─────────────────────────────────┘        │
│  [✨ 生成图片]                               │
├─────────────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐                 │
│  │ 生成图片1 │  │ 生成图片2 │                 │
│  └──────────┘  └──────────┘                 │
│     （历史记录）                              │
└─────────────────────────────────────────────┘
```

### 4.2 老师端 `/sk/ls`
```
┌─────────────────────────────────────────────┐
│  [Logo] 教师管理端   [刷新] [导入名单]      │
├─────────────────────────────────────────────┤
│  全班作品展示墙                              │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐      │
│  │张三名 │ │李四名 │ │王五名 │ │赵六名 │      │
│  │ 图片  │ │ 图片  │ │ 图片  │ │ 图片  │      │
│  └──────┘ └──────┘ └──────┘ └──────┘      │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐      │
│  │孙七名 │ │周八名 │ │...   │ │       │      │
│  │ 图片  │ │ 图片  │ │       │ │       │      │
│  └──────┘ └──────┘ └──────┘ └──────┘      │
└─────────────────────────────────────────────┘
```

## 5. 数据模型

### 学生表（students）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增 ID |
| name | TEXT | 姓名 |
| group_id | INTEGER | 组号（1-8） |
| created_at | DATETIME | 创建时间 |

### 图片生成记录表（images）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增 ID |
| student_id | INTEGER FK | 学生 ID |
| student_name | TEXT | 学生姓名 |
| group_id | INTEGER | 组号 |
| prompt | TEXT | 提示词（语音转文字） |
| image_url | TEXT | 生成的图片 URL |
| created_at | DATETIME | 生成时间 |

## 6. 非功能需求
- 响应式布局，适配平板和台式机屏幕
- 语音识别使用浏览器原生 Web Speech API（Chrome 支持最佳）
- 图片生成使用已部署的即梦 API（`http://localhost:8001/v1/chat/completions`）
- 数据持久化使用 SQLite（无需额外安装数据库）
- 部署路径 `/sk`，Nginx 反向代理

## 7. 待确认问题
- [ ] 班级人数上限？假设 40 人以内
- [ ] 是否需要学生登录密码？不需要
- [ ] 生成的图片是否需要下载到本地？老师端查看即可
